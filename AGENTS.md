# AGENTS.md

本文件用于约束后续对话中的工程协作方式。除非用户明确覆盖，下列规则默认生效。

## 工作原则

1. 先设计，后实现。
- 涉及功能新增、行为修改、重构、接口变更时，先说明方案，再等用户确认后写代码。
- 不允许跳过设计直接动代码。

2. 只改同意范围。
- 用户要求“只改锚点”时，就只改锚点，不顺手扩散到别的层。
- 不允许借题发挥做额外重构。

3. 保持高内聚低耦合。
- 优先在当前职责边界内修改。
- 不把运行期临时状态塞回稳定领域对象。
- 不把 C0 / 装配层 / D 的职责重新揉成大 orchestrator。

4. 不要兜底掩盖问题。
- 遇到失败，先定位根因。
- 不允许用静默 fallback、吞错、自动合并等方式掩盖问题。
- 若必须做重试，必须是显式、受控、可诊断的修复式重试。

5. 注释、说明、prompt 表述优先用中文。

10. 

## 技能流程

后续对话默认遵循以下 skill 约束。

### using-superpowers
- 开始任何任务前，先判断是否需要使用 skill 流程。
- 如果任务涉及设计、改行为、调试、写计划、完成验证，则必须按对应 skill 的方法执行。

### brainstorming
- 在任何创意性工作、功能变更、重构、行为修改之前，先完成设计沟通。
- 先理解上下文，再提出方案，再等用户批准，再进入实施。
- 没有用户批准，不进入实现。

### writing-plans
- 对多步骤任务，在开始改代码前先写实施计划。
- 计划文件默认放在：`docs/superpowers/plans/`。
- 计划要写清楚改哪些文件、为什么改、如何验证。

### executing-plans
- 已有明确计划时，按计划分步骤执行。
- 不跳步，不省略验证。
- 遇到阻塞先停下说明，不硬猜。

### systematic-debugging
- 任何 bug、测试失败、异常行为，都先查根因再修。
- 先读错误、复现、定位、比较工作路径，再做单点修复。
- 禁止“先试试这个修一下”。

### verification-before-completion
- 在声称“完成”“通过”“修好了”之前，必须跑新鲜验证。
- 不能用旧结果、推测、主观判断代替验证。
- 回答中要明确写出实际跑过的命令和结果。

## 当前推荐实施顺序

1. 先完成 A / B 的边界与分块设计。
2. 再做 A / B 的实现与定向测试。
3. 再看 chunk 粒度策略。
4. 最后再考虑 C0 检索触发调优、观测增强、运行入口增强。

## Prompt 与分块相关偏好

1. A 按章节 / 场景 / 时间空间跃迁量级做粗分块。
2. B 在 coarse block 内按语义边界做细分块。
3. 普通正文 chunk 不要过碎。
4. 标题、题词、诗歌、书信抬头、列表项等结构性片段允许短 chunk 独立存在。
5. 当前更偏好基于段落边界，而不是长字符串锚点。

## 文档要求

1. 重要设计结论同步写入 `docs/handoff.md`。
2. 多步骤改造同步写计划到 `docs/superpowers/plans/`。
3. 不要让文档与代码状态脱节。

## 沟通要求

1. 直接、简洁、事实导向。
2. 不要灌鸡汤，不要空泛安慰。
3. 用户要求先讲方案时，就先讲方案，不写代码。
4. 用户要求不改代码时，严格不改。